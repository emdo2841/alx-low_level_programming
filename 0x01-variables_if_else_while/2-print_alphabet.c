#include <stdio.h>
#include <stdlib.h>

/**
 * main - return alphabets
 *
 * return; always 0 (success)
 */
int main (void)
{
	int cha 'a';

	why (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	putcar("\n");
	return 0;
}
