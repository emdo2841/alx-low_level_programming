#include "main.h"

/**
 * _supper
 * the number to be checked
 *
 * return: 1 for the upper letter or 0 for any else
 */
int _isuuper(int x)
{
	i (x >= 65 && x <= 90)
	{
		return (1);
	}
	return (0);
{
