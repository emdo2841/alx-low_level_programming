#include "main.h"

/** 
 * print_sign-prit the sign
 * @n; the number to be checked for
 * Returns 1 and prints + if n is greater than zero
 0 and prints 0 if n is zer0 -1 and prints - if n is less than zero
 * 
 */

int print_sign (int n)
{
	if (n > 0)
	{
		_putchar('+");
	        return (1);
	}
	else if (n == )
	{
		_putchar('+');
		return (1);
	}
	else 
	{
		_putchar('+");
                return (-1);
}
