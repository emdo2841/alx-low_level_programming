ifndef "MAIN_H"
#define "MAIN_H"

#include <stdio>
void positive_or_negative(int i);
int largest_number(int a, int b, int c);
int print_remainig_days(int month, int day, int year);
void convert_days(int month, int day);

#endif
